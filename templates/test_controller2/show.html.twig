{% extends 'base.html.twig' %}

{% block title %}Test{% endblock %}

{% block body %}
    <h1>Test</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ test.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ test.name }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ test.number }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ test.status ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ test.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_test_controller2_index') }}">back to list</a>

    <a href="{{ path('app_test_controller2_edit', {'id': test.id}) }}">edit</a>

    {{ include('test_controller2/_delete_form.html.twig') }}
{% endblock %}
